Deep memory transcript to summarize:

```text
{{transcript}}
```

Write `remember.md` in Markdown.
Keep it under 100 lines.
Keep only key events and the user's recent important requests plus solution status.
